# hamza00003.github.io

## Run locally

From the project folder, run:

```powershell
.\start-local.ps1
```

Then open http://localhost:8000 in your browser. The site must be served over HTTP because the page loads its HTML components with `fetch()`.

You can also run `python -m http.server 8000` directly.